#ifndef UI_MENU_CUBEMAP_H
#define UI_MENU_CUBEMAP_H

#include "engine.h"

void draw_menu_cubemap(engine_t* const e);
void handle_menu_cubemap(engine_t* const e);
void init_menu_cubemap(engine_t* const e);

#endif
