#ifndef UI_MENU_DEBUG_H
#define UI_MENU_DEBUG_H

#include "engine.h"

void init_menu_debug(engine_t* const e);
void handle_menu_debug(engine_t* const e);
void draw_menu_debug(engine_t* const e);

#endif
