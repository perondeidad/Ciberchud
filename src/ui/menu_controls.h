#ifndef UI_MENU_CONTROLS_H
#define UI_MENU_CONTROLS_H

#include "engine.h"

void init_menu_controls(engine_t* const e);
void handle_menu_controls(engine_t* const e);
void draw_menu_controls(engine_t* const e);

#endif
