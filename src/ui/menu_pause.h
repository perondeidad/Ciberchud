#ifndef UI_MENU_PAUSE_H
#define UI_MENU_PAUSE_H

#include "engine.h"

void init_menu_pause(engine_t* const e);
void handle_menu_pause(engine_t* const e);
void draw_menu_pause(engine_t* const e);

#endif
