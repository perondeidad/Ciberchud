#ifndef UI_MENU_OPTIONS_H
#define UI_MENU_OPTIONS_H

#include "engine.h"

void init_menu_options(engine_t* const e);
void handle_menu_options(engine_t* const e);
void draw_menu_options(engine_t* const e);

#endif
